
export interface TaskData {
  title: string;
  description: string;
  status: string;
  priority: string;
}

export interface UserData {
  name: string;
  email: string;
  password: string;
  confirmpassword: string;
  age:string
}